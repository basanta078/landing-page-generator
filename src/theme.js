import { createTheme } from '@mui/material';

const theme = createTheme({
    palette: {
        primary: {
            main: "#97afa3"
        },
        secondary: {
            main: "#64646d"
        }
    }
})

export default theme;